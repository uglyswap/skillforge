# Sécurité (OBLIGATOIRE — générer avec: openssl rand -hex 32)
JWT_SECRET=
ENCRYPTION_KEY=

# URL publique de l'app (pour les headers OpenRouter)
APP_URL=http://localhost:3737

# Base de données SQLite (ne pas modifier en production Docker)
DATABASE_URL="file:../data/skillforge.db"
